# homebrew-umbra-gate

Homebrew tap for `umbra-gate`.

## Install

```bash
brew tap jachy-h/umbra-gate
brew install umbra-gate
```

## Development

This tap currently contains the formula skeleton. Release URLs and SHA256 values
should be updated when GitHub release artifacts are published from
`jachy-h/umbra-gate`.
